#include<iostream.h>
#include<conio.h>
int area(int l,int b);
float area(float r);
int area(int s);
float area(int base,float h);
void main()
{
	clrscr();
	int l,b;
	cout<<"enter the length and breadth\n";
	cin>>l>>b;
	cout<<"area of rectangle is"<<area(l,b)<<endl;;
	float r;
	cout<<"\nenter the radius\n";
	cin>>r;
	cout<<"area of circle is"<<area(r)<<endl;
	int s;

	cout<<"enter the side\n";
	cin>>s;
	cout<<"area of square is"<<area(s)<<endl;
	int base;
	float h;
	cout<<"enter the base  and  heigth\n";
	cin>>base>>h;
	cout<<"area of tringle is"<<area(base,h)<<endl;
	getch();
}
int area(int l,int b)
{
	return(l*b);
}
float area(float r)
{
	return(3.14*r*r);
}
int area(int s)
{
	return(s*s);
}
float area(int b,float h)
{
	return(0.5*b*h);
}


